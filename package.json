{
  "name": "even-g2-openclaw-chat-app",
  "version": "0.1.0",
  "type": "module",
  "description": "EvenHub companion app for Even G2 ↔ OpenClaw voice interactions",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.1.0",
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0"
  }
}
