---
phase: 11-glasses-command-menu
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/menu/command-menu.ts
  - src/gestures/gesture-fsm.ts
  - src/types.ts
  - src/__tests__/command-menu.test.ts
  - src/__tests__/gesture-fsm.test.ts
autonomous: true
requirements: [MENU-01, MENU-02, MENU-03, MENU-04]

must_haves:
  truths:
    - "CommandMenu state module provides pure-function transitions for scroll, select, and confirm"
    - "renderMenuText produces display-ready plain text with cursor indicator and confirmation dialog"
    - "Gesture FSM emits MENU_SELECT on tap-in-menu instead of dismissing to idle"
    - "Destructive menu items trigger confirmation sub-state before execution"
  artifacts:
    - path: "src/menu/command-menu.ts"
      provides: "Pure menu state management with scroll, select, confirm, cancel"
      exports: ["createMenuState", "menuScrollUp", "menuScrollDown", "menuSelect", "menuCancelConfirm", "renderMenuText", "MENU_ITEMS", "MenuState", "MenuItem", "MenuCommand", "MenuSelectResult"]
    - path: "src/__tests__/command-menu.test.ts"
      provides: "TDD test suite for command menu state transitions"
      contains: "describe.*CommandMenu"
    - path: "src/gestures/gesture-fsm.ts"
      provides: "Extended FSM with MENU_SELECT action"
      contains: "MENU_SELECT"
    - path: "src/types.ts"
      provides: "Menu event types in AppEventMap"
      contains: "menu:select"
  key_links:
    - from: "src/menu/command-menu.ts"
      to: "src/menu/menu-controller.ts"
      via: "menu state consumed by controller (Plan 11-02)"
      pattern: "import.*command-menu"
    - from: "src/gestures/gesture-fsm.ts"
      to: "src/gestures/gesture-handler.ts"
      via: "MENU_SELECT action dispatched by handler (Plan 11-02)"
      pattern: "MENU_SELECT"
---

<objective>
Create the pure command menu state module with TDD and extend the gesture FSM with a MENU_SELECT action.

Purpose: Establish the foundational menu logic (items, scroll, select, confirm/cancel, text rendering) as pure functions with comprehensive tests, and prepare the FSM to emit a selectable action instead of dismissing the menu on tap.

Output: `src/menu/command-menu.ts` (pure state module), `src/__tests__/command-menu.test.ts` (test suite), modified `src/gestures/gesture-fsm.ts` (MENU_SELECT action), modified `src/types.ts` (menu event types)
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-glasses-command-menu/11-RESEARCH.md

@src/gestures/gesture-fsm.ts
@src/types.ts
@src/__tests__/gesture-fsm.test.ts
</context>

<feature>
  <name>CommandMenu Pure State Module + FSM Extension</name>
  <files>src/menu/command-menu.ts, src/__tests__/command-menu.test.ts, src/gestures/gesture-fsm.ts, src/types.ts</files>
  <behavior>
    CommandMenu state management (pure functions, no side effects):

    1. createMenuState() -> MenuState with 5 items, selectedIndex=0, confirmingIndex=null
    2. menuScrollUp({ selectedIndex: 2 }) -> { selectedIndex: 1 }
    3. menuScrollUp({ selectedIndex: 0 }) -> { selectedIndex: 0 } (clamped)
    4. menuScrollDown({ selectedIndex: 2 }) -> { selectedIndex: 3 }
    5. menuScrollDown({ selectedIndex: 4 }) -> { selectedIndex: 4 } (clamped)
    6. menuScrollUp/Down while confirmingIndex !== null -> no change (scroll locked during confirm)
    7. menuSelect on non-destructive item -> { action: 'execute', command: 'new' }
    8. menuSelect on destructive item -> { action: 'confirm' }, state.confirmingIndex set
    9. menuSelect while confirmingIndex !== null -> { action: 'execute', command: item.command }
    10. menuCancelConfirm -> confirmingIndex reset to null
    11. renderMenuText normal state -> header + 5 items with '> ' cursor + footer
    12. renderMenuText confirm state -> item label + "Are you sure?" + instructions

    MENU_ITEMS array (5 items, in order):
    - { id: 'new', label: '/new     New session', command: 'new', destructive: false }
    - { id: 'switch', label: '/switch  Switch session', command: 'switch', destructive: false }
    - { id: 'rename', label: '/rename  Rename session', command: 'rename', destructive: false }
    - { id: 'reset', label: '/reset   Clear messages', command: 'reset', destructive: true }
    - { id: 'delete', label: '/delete  Delete session', command: 'delete', destructive: true }

    Gesture FSM extension:
    - GestureAction union gains { type: 'MENU_SELECT' }
    - menu.tap transition changes from { nextState: 'idle', action: null } to { nextState: 'menu', action: { type: 'MENU_SELECT' } }
    - Existing test for menu.tap must be updated to expect MENU_SELECT action and nextState='menu'

    AppEventMap extension:
    - Add 'menu:select': { timestamp: number }
    - Add 'menu:close': {}
  </behavior>
  <implementation>
    RED phase:
    1. Create src/__tests__/command-menu.test.ts with tests for all 12 behaviors above
    2. Create src/menu/command-menu.ts as a stub that exports all types and functions (throwing 'not implemented')
    3. Run tests -- all must FAIL (red)

    GREEN phase:
    1. Implement all pure functions in src/menu/command-menu.ts following research Pattern 1
    2. Run tests -- all must PASS (green)

    FSM extension (standard, not TDD -- small change to existing tested module):
    1. Add { type: 'MENU_SELECT' } to GestureAction union in gesture-fsm.ts
    2. Change menu.tap from { nextState: 'idle', action: null } to { nextState: 'menu', action: { type: 'MENU_SELECT' } }
    3. Update existing gesture-fsm.test.ts: change the "tap -> idle, null" test to expect "tap -> menu, MENU_SELECT"
    4. Add 'menu:select' and 'menu:close' event types to AppEventMap in types.ts
    5. Run all tests (npx vitest run) -- all must pass
  </implementation>
</feature>

<verification>
```bash
npx vitest run src/__tests__/command-menu.test.ts src/__tests__/gesture-fsm.test.ts --reporter=verbose
```
- command-menu.test.ts: all tests pass (scroll, select, confirm, cancel, render)
- gesture-fsm.test.ts: menu.tap test expects { nextState: 'menu', action: { type: 'MENU_SELECT' } }
- No other test regressions: `npx vitest run`
</verification>

<success_criteria>
- src/menu/command-menu.ts exports all types and pure functions
- src/__tests__/command-menu.test.ts covers all 12 behaviors
- gesture-fsm.ts has MENU_SELECT in GestureAction and menu.tap transition
- types.ts has menu:select and menu:close events
- All project tests pass (npx vitest run)
</success_criteria>

<output>
After completion, create `.planning/phases/11-glasses-command-menu/11-01-SUMMARY.md`
</output>
