---
phase: 19-test-infrastructure-resilience-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/__tests__/integration/sync-resilience.test.ts
  - src/__tests__/integration/error-escalation.test.ts
autonomous: true
requirements:
  - RES-21

must_haves:
  truths:
    - "Integration test verifies lossy sync bridge causes drift detection after consecutive mismatches"
    - "Integration test verifies drift reconciler triggers reconciliation callback on detected drift"
    - "Integration test verifies write failure on auto-save triggers persistence:error event after retry exhaustion"
    - "Integration test verifies createQuotaExceededStore causes immediate escalation on first write"
    - "All new and existing tests pass together without regressions"
  artifacts:
    - path: "src/__tests__/integration/sync-resilience.test.ts"
      provides: "Integration tests for sync message loss, drift detection, and reconciliation"
      min_lines: 80
    - path: "src/__tests__/integration/error-escalation.test.ts"
      provides: "Integration tests for write failure retry exhaustion and error escalation"
      min_lines: 80
  key_links:
    - from: "src/__tests__/integration/sync-resilience.test.ts"
      to: "src/__tests__/helpers/failure-helpers.ts"
      via: "createLossySyncBridge import"
      pattern: "createLossySyncBridge"
    - from: "src/__tests__/integration/sync-resilience.test.ts"
      to: "src/sync/drift-reconciler.ts"
      via: "createDriftReconciler factory"
      pattern: "createDriftReconciler"
    - from: "src/__tests__/integration/sync-resilience.test.ts"
      to: "src/sync/sync-monitor.ts"
      via: "createSyncMonitor factory"
      pattern: "createSyncMonitor"
    - from: "src/__tests__/integration/error-escalation.test.ts"
      to: "src/__tests__/helpers/failure-helpers.ts"
      via: "createFailingStore and createQuotaExceededStore imports"
      pattern: "createFailingStore|createQuotaExceededStore"
    - from: "src/__tests__/integration/error-escalation.test.ts"
      to: "src/persistence/auto-save.ts"
      via: "createAutoSave factory"
      pattern: "createAutoSave"
    - from: "src/__tests__/integration/error-escalation.test.ts"
      to: "src/events.ts"
      via: "createEventBus factory"
      pattern: "createEventBus"
---

<objective>
Create integration tests for sync resilience and error escalation flows, completing the Phase 19 test coverage.

Purpose: This plan completes RES-21 by adding the remaining two integration test suites: (1) sync message loss leading to drift detection and reconciliation, and (2) auto-save write failure leading to retry exhaustion and error escalation via event bus. These tests compose real modules with failure helpers created in Plan 01.

Output: sync-resilience.test.ts and error-escalation.test.ts with integration tests verifying multi-module resilience scenarios.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-test-infrastructure-resilience-coverage/19-RESEARCH.md
@.planning/phases/19-test-infrastructure-resilience-coverage/19-01-SUMMARY.md
@src/sync/sync-types.ts
@src/sync/drift-reconciler.ts
@src/sync/sync-monitor.ts
@src/persistence/auto-save.ts
@src/persistence/conversation-store.ts
@src/persistence/db.ts
@src/events.ts
@src/types.ts
@src/__tests__/helpers/failure-helpers.ts
@src/__tests__/sync-monitor.test.ts
@src/__tests__/drift-reconciler.test.ts
@src/__tests__/auto-save.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync resilience integration tests</name>
  <files>src/__tests__/integration/sync-resilience.test.ts</files>
  <action>
Create `src/__tests__/integration/sync-resilience.test.ts` with integration tests composing real SyncMonitor + DriftReconciler + ConversationStore to test the drift detection and reconciliation flow.

Required imports:
- `import 'fake-indexeddb/auto'` (must be first import)
- `import { openDB, closeDB, DB_NAME } from '../../persistence/db'`
- `import { createConversationStore } from '../../persistence/conversation-store'`
- `import { createDriftReconciler } from '../../sync/drift-reconciler'`
- `import { createSyncMonitor } from '../../sync/sync-monitor'`
- `import { createLossySyncBridge } from '../helpers/failure-helpers'`
- SyncBridge and SyncMessage types from `../../sync/sync-types`

Standard afterEach: close DB + deleteDatabase + vi.useRealTimers() if fake timers used.

Test suite: `describe('sync resilience integration', () => { ... })`

Tests to implement:

1. **'createLossySyncBridge drops every Nth message'**
   - Create a simple mock SyncBridge (messages array + postMessage pushes to it)
   - Wrap with createLossySyncBridge(mockBridge, 3)
   - Post 6 messages through the lossy bridge
   - Assert the real bridge received 4 messages (messages 3 and 6 were dropped)

2. **'drift reconciler detects count mismatch and triggers reconciliation'**
   - Use vi.useFakeTimers()
   - Open DB, create store, create conversation, add 3 messages
   - Create drift reconciler with real store and an onReconcile callback (vi.fn())
   - Call handleHeartbeat with a remote count that differs from local (e.g., remoteCount=5 for the conversation)
   - Call handleHeartbeat again with same mismatch (2-consecutive-mismatch rule per Decision log)
   - Assert the onReconcile callback was called with the conversationId
   - Cleanup: destroy reconciler

3. **'drift reconciler ignores single mismatch (no false positive)'**
   - Same setup as test 2 but only send ONE mismatched heartbeat
   - Then send a matching heartbeat
   - Assert onReconcile was NOT called (single mismatch = no action per 2-consecutive rule)

4. **'sync monitor tracks heartbeat and reports alive status'**
   - Use vi.useFakeTimers()
   - Create a mock SyncBridge with a working onMessage handler
   - Create SyncMonitor with the bridge and a real store
   - Verify isAlive() returns true initially (no heartbeat = peer may not have started, per Decision)
   - Simulate receiving a heartbeat message via the onMessage handler
   - Verify isAlive() still returns true
   - Advance time past 30 seconds without another heartbeat
   - Verify isAlive() returns false
   - Cleanup: destroy monitor

Read the existing sync-monitor.test.ts and drift-reconciler.test.ts to understand the factory signatures and follow the same patterns. The SyncMonitor factory requires `{ syncBridge, store, getActiveConversationId }`. The DriftReconciler factory requires `{ store, onReconcile }`.
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx vitest run src/__tests__/integration/sync-resilience.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 4 sync resilience integration tests pass. Lossy bridge drops correctly, drift reconciler triggers on 2-consecutive mismatches, ignores single mismatches, sync monitor tracks alive status.</done>
</task>

<task type="auto">
  <name>Task 2: Create error escalation integration tests</name>
  <files>src/__tests__/integration/error-escalation.test.ts</files>
  <action>
Create `src/__tests__/integration/error-escalation.test.ts` with integration tests composing real AutoSave + EventBus + ConversationStore with failure helpers to test the write failure -> retry exhaustion -> error escalation flow.

Required imports:
- `import 'fake-indexeddb/auto'` (must be first import)
- `import { openDB, closeDB, DB_NAME } from '../../persistence/db'`
- `import { createConversationStore } from '../../persistence/conversation-store'`
- `import { createAutoSave } from '../../persistence/auto-save'`
- `import { createEventBus } from '../../events'`
- `import type { AppEventMap } from '../../types'`
- `import { createFailingStore, createQuotaExceededStore } from '../helpers/failure-helpers'`

CRITICAL: Use `vi.useFakeTimers()` for ALL tests in this suite. Auto-save uses setTimeout for retry delays (500ms, 1000ms, 1500ms). Use `vi.advanceTimersByTimeAsync()` (ASYNC version -- see Research Pitfall 3) to advance through retries since IDB uses microtasks.

Standard afterEach: close DB + deleteDatabase + vi.useRealTimers().

Test suite: `describe('error escalation integration', () => { ... })`

Tests to implement:

1. **'write failure triggers persistence:warning after retries'**
   - vi.useFakeTimers()
   - Open DB, create real store, create a conversation
   - Wrap store with createFailingStore(realStore, 0) -- fails immediately on all writes
   - Create event bus, subscribe to 'persistence:warning'
   - Create autoSave with { bus, store: failStore, getConversationId: () => conv.id }
   - Emit 'gateway:chunk' with { type: 'transcript', text: 'Will fail' }
   - Advance timers through retry cycle: `for (let i = 0; i < 10; i++) { await vi.advanceTimersByTimeAsync(500); }`
   - Assert persistence:warning was emitted with message 'Messages may not be saved'
   - Destroy autoSave

2. **'write failure triggers persistence:error after retry exhaustion'**
   - Same setup as test 1
   - Subscribe to 'persistence:error' instead
   - Emit gateway:chunk, advance through full retry cycle
   - Assert persistence:error was emitted with type 'write-failed' and recoverable: false
   - Destroy autoSave

3. **'quota exceeded triggers persistence:error with quota-exceeded type'**
   - vi.useFakeTimers()
   - Open DB, create real store, create a conversation
   - Wrap store with createQuotaExceededStore(realStore)
   - Create event bus, subscribe to 'persistence:error'
   - Create autoSave with quota-exceeded store
   - Emit 'gateway:chunk' with transcript
   - Advance timers through retry cycle
   - Assert persistence:error was emitted (the DOMException name 'QuotaExceededError' will be caught by auto-save)
   - Destroy autoSave

4. **'successful write emits no error events'**
   - vi.useFakeTimers()
   - Open DB, create real store (NOT failing), create conversation
   - Create event bus, subscribe to both persistence:warning and persistence:error
   - Create autoSave with real store
   - Emit gateway:chunk with transcript, advance timers
   - Assert neither persistence:warning nor persistence:error was emitted
   - Destroy autoSave

5. **'partial write succeeds before failAfterN threshold'**
   - vi.useFakeTimers()
   - Open DB, create real store, create conversation
   - Wrap with createFailingStore(realStore, 1) -- allows first write
   - Create event bus, create autoSave
   - Emit gateway:chunk with transcript text
   - Advance timers to let first save complete
   - Verify the message was persisted (store.getMessages returns 1 message)
   - Emit another gateway:chunk
   - Advance timers through retry cycle
   - Assert persistence:warning or persistence:error emitted for the second save attempt
   - Destroy autoSave

Read the existing auto-save.test.ts (especially lines 158-184) for the established pattern of testing auto-save with failing stores and timer advancement.
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx vitest run src/__tests__/integration/error-escalation.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 5 error escalation integration tests pass. Write failures escalate through retry to persistence:warning and persistence:error. Quota exceeded is handled. Successful writes emit no errors. Partial write threshold works correctly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/integration/` -- all integration tests pass
2. `npx vitest run` -- full test suite (484 existing + new) passes with no regressions
3. Verify test count increased by checking `npx vitest run --reporter=verbose 2>&1 | tail -5`
</verification>

<success_criteria>
- sync-resilience.test.ts has 4 passing integration tests covering lossy bridge, drift detection, false positive prevention, and monitor alive tracking
- error-escalation.test.ts has 5 passing integration tests covering write failure escalation, quota exceeded, no false error events, and partial write behavior
- Full test suite passes with 0 failures and test count exceeds 484
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-infrastructure-resilience-coverage/19-02-SUMMARY.md`
</output>
