---
phase: 13-phase9-verification-sync-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-indexeddb-persistence/09-VERIFICATION.md
  - src/persistence/auto-save.ts
  - src/glasses-main.ts
  - src/hub-main.ts
  - src/__tests__/auto-save.test.ts
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, SYNC-01]
gap_closure: true

must_haves:
  truths:
    - "Phase 9 VERIFICATION.md exists and confirms PERS-01 through PERS-04 pass with code evidence"
    - "onConversationNamed callback in glasses-main.ts posts conversation:named SyncMessage via syncBridge (not a no-op stub)"
    - "Auto-save posts message:added SyncMessage via syncBridge after each successful user and assistant message save"
    - "Hub sync handler receives and dispatches both conversation:named and message:added message types"
    - "All existing tests continue to pass (syncBridge is optional in AutoSaveOptions)"
  artifacts:
    - path: ".planning/phases/09-indexeddb-persistence/09-VERIFICATION.md"
      provides: "Formal Phase 9 persistence verification with per-requirement evidence"
      contains: "PERS-01"
    - path: "src/persistence/auto-save.ts"
      provides: "Sync bridge integration for cross-context message notifications"
      contains: "syncBridge"
    - path: "src/glasses-main.ts"
      provides: "Wired onConversationNamed callback posting conversation:named"
      contains: "conversation:named"
    - path: "src/hub-main.ts"
      provides: "Extended sync handler with conversation:named and message:added cases"
      contains: "conversation:named"
  key_links:
    - from: "src/persistence/auto-save.ts"
      to: "src/sync/sync-types.ts"
      via: "syncBridge.postMessage({type:'message:added'})"
      pattern: "postMessage.*message:added"
    - from: "src/glasses-main.ts"
      to: "src/sync/sync-bridge.ts"
      via: "syncBridge.postMessage({type:'conversation:named'})"
      pattern: "postMessage.*conversation:named"
    - from: "src/hub-main.ts"
      to: "syncBridge.onMessage handler"
      via: "case 'conversation:named' and case 'message:added' in switch"
      pattern: "case 'conversation:named'"
---

<objective>
Formally verify Phase 9 persistence requirements (PERS-01 through PERS-04) by creating the missing VERIFICATION.md, then wire the two dead-code SyncMessage types (message:added and conversation:named) so that the glasses context posts them through the syncBridge after persisting data, and the hub context handles them.

Purpose: Close the gap identified by the v1.2 milestone audit -- Phase 9 work is done but unverified, and two sync message types are defined but never posted. This unblocks Phase 12's real-time hub features.
Output: 09-VERIFICATION.md, updated auto-save.ts with syncBridge posting, updated glasses-main.ts with wired onConversationNamed, updated hub-main.ts with new sync cases, updated auto-save tests.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-phase9-verification-sync-wiring/13-RESEARCH.md
@.planning/phases/09-indexeddb-persistence/09-01-SUMMARY.md
@.planning/phases/09-indexeddb-persistence/09-02-SUMMARY.md
@.planning/phases/10-dynamic-sessions-cross-context-sync/10-VERIFICATION.md

@src/persistence/auto-save.ts
@src/glasses-main.ts
@src/hub-main.ts
@src/sync/sync-types.ts
@src/__tests__/auto-save.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 9 VERIFICATION.md confirming PERS-01 through PERS-04</name>
  <files>.planning/phases/09-indexeddb-persistence/09-VERIFICATION.md</files>
  <action>
Create a formal verification document for Phase 9 following the same format as the Phase 10 VERIFICATION.md (at `.planning/phases/10-dynamic-sessions-cross-context-sync/10-VERIFICATION.md`).

**Scope: ONLY verify PERS-01 through PERS-04.** Do NOT verify SESS-* or SYNC-* requirements (those belong to Phase 10).

For each requirement, inspect the actual source files and test files to gather evidence:

**PERS-01** (Conversations persist in IndexedDB across page reloads and app restarts):
- Verify `src/persistence/conversation-store.ts` has createConversation, getConversation, getAllConversations, getLastConversation
- Verify `src/__tests__/conversation-store.test.ts` tests exist and reference IndexedDB operations
- Verify `src/persistence/db.ts` has openDB with schema versioning
- Verify `src/persistence/boot-restore.ts` restores from IndexedDB on boot

**PERS-02** (Messages auto-save as they arrive):
- Verify `src/persistence/auto-save.ts` subscribes to `gateway:chunk` events
- Verify transcript case saves user messages, response_end case saves assistant messages
- Verify `src/__tests__/auto-save.test.ts` has tests for both save paths
- Verify retry logic exists (MAX_RETRIES = 3)

**PERS-03** (Active conversation loads automatically on app boot):
- Verify `src/persistence/boot-restore.ts` reads localStorage pointer then loads from IndexedDB
- Verify `src/__tests__/boot-restore.test.ts` tests exist for restore scenarios
- Verify `src/glasses-main.ts` calls `restoreOrCreateConversation` at boot and replays messages into renderer

**PERS-04** (Conversation metadata auto-generated):
- Verify `src/persistence/conversation-store.ts` has `generateConversationName()`
- Verify auto-save calls `store.updateConversation(convId, { name })` on first user message
- Note the gap: `onConversationNamed` callback is currently a no-op stub (will be fixed in Task 2)

Include sections: Observable Truths table, Required Artifacts table, Key Links table, Requirements Coverage table. Use VERIFIED/PARTIAL status. Mark PERS-04 as PARTIAL with note that the onConversationNamed callback is a stub (Task 2 of this phase will fix it).

Run `npx vitest run` to confirm all tests pass, and report the count in the verification document.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-indexeddb-persistence/09-VERIFICATION.md && grep -q "PERS-01" .planning/phases/09-indexeddb-persistence/09-VERIFICATION.md && grep -q "PERS-04" .planning/phases/09-indexeddb-persistence/09-VERIFICATION.md && echo "PASS"</automated>
    <manual>09-VERIFICATION.md covers all 4 PERS requirements with code evidence</manual>
  </verify>
  <done>09-VERIFICATION.md exists with VERIFIED status for PERS-01, PERS-02, PERS-03 and PARTIAL for PERS-04 (stub noted). All evidence references real file paths and line numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire syncBridge into auto-save, onConversationNamed, and hub sync handler</name>
  <files>
    src/persistence/auto-save.ts
    src/glasses-main.ts
    src/hub-main.ts
    src/__tests__/auto-save.test.ts
  </files>
  <action>
Make 3 surgical changes to wire the dead-code sync message types. Follow the patterns from 13-RESEARCH.md exactly.

**1. auto-save.ts -- Add optional syncBridge to AutoSaveOptions and post message:added after successful saves:**

- Import `SyncBridge` type from `../sync/sync-types`
- Add `syncBridge?: SyncBridge` to `AutoSaveOptions` interface (MUST be optional to avoid breaking existing tests)
- Destructure `syncBridge` from opts in `createAutoSave`
- In the transcript case: after `saveWithRetry` succeeds (inside `.then((ok) => { if (ok && syncBridge) { ... } })`), call `syncBridge.postMessage({ type: 'message:added', origin: 'glasses', conversationId: convId, role: 'user', text })`
- In the response_end case: after `saveWithRetry` succeeds, call `syncBridge.postMessage({ type: 'message:added', origin: 'glasses', conversationId: convId, role: 'assistant', text })`
- CRITICAL: Post sync message ONLY after save succeeds (`if (ok && syncBridge)`), never before. This prevents the hub from showing messages that failed to persist.

**2. glasses-main.ts -- Wire onConversationNamed callback:**

- Replace the no-op `onConversationNamed: (_name) => { // Future: update UI ... }` stub (around line 187) with a real implementation that posts the conversation:named SyncMessage:
  ```
  onConversationNamed: (name) => {
    syncBridge.postMessage({
      type: 'conversation:named',
      origin: 'glasses',
      conversationId: activeConversationId,
      name,
    });
  },
  ```
- Also pass `syncBridge` to the `createAutoSave` options: add `syncBridge,` to the options object (around line 183-190).

**3. hub-main.ts -- Extend sync message handler:**

- In the `syncBridge.onMessage` handler (around line 566-577), add two new cases to the switch statement:
  - `case 'conversation:named':` falls through to `refreshSessionList()` (same as other session events -- the session list shows conversation names)
  - `case 'message:added':` with a `break` and comment `// Phase 12 will use this for live conversation view (HUB-01)` -- acknowledges the message but takes no action yet

**4. auto-save.test.ts -- Add test for syncBridge posting:**

- Add 1-2 new tests to the existing test suite:
  - "posts message:added via syncBridge after successful user message save": Create a mock syncBridge `{ postMessage: vi.fn(), onMessage: vi.fn(() => () => {}), destroy: vi.fn() }`, pass it to createAutoSave, emit a gateway:chunk transcript event, flush promises, assert `syncBridge.postMessage` was called with `{ type: 'message:added', origin: 'glasses', conversationId: expect.any(String), role: 'user', text: expect.any(String) }`
  - "posts message:added via syncBridge after successful assistant message save": Emit response_delta then response_end, flush promises, assert postMessage called with role 'assistant'
  - "does not post sync message when syncBridge is not provided": Verify existing tests still work without syncBridge (no crash, no postMessage call)
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx vitest run 2>&1 | tail -20</automated>
    <manual>grep for 'message:added' in auto-save.ts confirms sync posting; grep for 'conversation:named' in glasses-main.ts confirms callback wiring; grep for 'conversation:named' in hub-main.ts confirms handler extension</manual>
  </verify>
  <done>
All 3 sync wiring changes complete:
- auto-save.ts posts message:added for both user (transcript) and assistant (response_end) messages after successful save
- glasses-main.ts onConversationNamed callback posts conversation:named via syncBridge (no longer a stub)
- hub-main.ts sync handler has cases for both conversation:named (refreshes session list) and message:added (acknowledged for Phase 12)
- New auto-save tests verify syncBridge posting behavior
- All existing tests continue to pass (syncBridge remains optional)
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (325+ existing + new auto-save sync tests)
2. `npx tsc --noEmit` -- TypeScript compiles clean with no type errors
3. `grep -n "postMessage.*message:added" src/persistence/auto-save.ts` -- confirms sync posting wired
4. `grep -n "conversation:named" src/glasses-main.ts` -- confirms callback wired (not a stub)
5. `grep -n "conversation:named" src/hub-main.ts` -- confirms hub handler extended
6. `.planning/phases/09-indexeddb-persistence/09-VERIFICATION.md` exists with PERS-01 through PERS-04 evidence
7. No `_name` unused parameter in glasses-main.ts onConversationNamed callback (stub replaced)
</verification>

<success_criteria>
- Phase 9 VERIFICATION.md confirms all 4 PERS requirements with code-level evidence
- conversation:named and message:added are no longer dead code -- posted by glasses context, handled by hub context
- All existing and new tests pass
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-phase9-verification-sync-wiring/13-01-SUMMARY.md`
</output>
