---
phase: 08-evenhub-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - package.json
  - vite.config.ts
autonomous: true
requirements:
  - SUB-01
  - SUB-02
  - SUB-03
  - SUB-04

must_haves:
  truths:
    - "app.json defines package identity, version, description, permissions (network wildcard, microphone), and entrypoint that passes evenhub pack validation"
    - "Production build produces only index.html and assets/ in dist/ -- no preview-glasses.html"
    - "Production build uses relative asset paths (./assets/) not absolute (/assets/)"
    - "Running npm run pack produces a .ehpk artifact file"
    - "Dev server still serves preview-glasses.html at http://localhost:3200/preview-glasses.html"
  artifacts:
    - path: "app.json"
      provides: "EvenHub package metadata"
      contains: "package_id"
    - path: "vite.config.ts"
      provides: "Conditional rollup inputs and relative base path"
      contains: "base: './' "
    - path: "package.json"
      provides: "Pack npm script and evenhub-cli devDependency"
      contains: "evenhub pack"
    - path: "openclaw-chat.ehpk"
      provides: "EvenHub submission artifact (produced by npm run pack)"
  key_links:
    - from: "package.json"
      to: "app.json"
      via: "pack script references app.json as first argument"
      pattern: "evenhub pack app\\.json"
    - from: "app.json"
      to: "dist/index.html"
      via: "entrypoint field must match a file in dist/"
      pattern: "\"entrypoint\": \"index.html\""
    - from: "vite.config.ts"
      to: "dist/"
      via: "build output with conditional inputs and relative base"
      pattern: "base: '\\./' "
---

<objective>
Create the EvenHub submission configuration: app.json metadata, production build tuning (simulator exclusion + relative paths), evenhub-cli installation, and pack npm script.

Purpose: Package the completed app as a valid .ehpk artifact for EvenHub portal submission.
Output: app.json, modified vite.config.ts, modified package.json, and a working `npm run pack` that produces `openclaw-chat.ehpk`.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-evenhub-submission/08-RESEARCH.md

@vite.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app.json and add evenhub-cli + pack script to package.json</name>
  <files>app.json, package.json</files>
  <action>
1. Create `app.json` at project root with exactly this content:
```json
{
  "package_id": "lv.kingdom.bibele.openclawchat",
  "edition": "202601",
  "name": "OpenClaw Chat",
  "version": "0.1.0",
  "min_app_version": "0.1.0",
  "tagline": "Voice chat with OpenClaw AI on Even G2",
  "description": "EvenHub companion app for Even G2 that provides voice/chat UX and connects to an OpenClaw backend gateway for STT + AI responses.",
  "author": "bibele.kingdom.lv",
  "entrypoint": "index.html",
  "permissions": {
    "network": ["*"],
    "microphone": true
  }
}
```

**Important deviations from user's CONTEXT.md decisions (forced by CLI validation):**
- `package_id` uses `openclawchat` (no hyphen) instead of user's `openclaw-chat` -- the evenhub-cli v0.1.5 regex `^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)*$` rejects hyphens. Validated empirically.
- `edition` uses `"202601"` instead of user's `"202602"` -- the CLI Zod schema `z.enum(["202601"])` only accepts this value. Validated empirically.

2. Install `@evenrealities/evenhub-cli` as a devDependency:
```bash
npm install --save-dev @evenrealities/evenhub-cli@^0.1.5
```

3. Add `pack` script to package.json scripts section:
```json
"pack": "npm run build && evenhub pack app.json dist -o openclaw-chat.ehpk"
```
This follows the convention used by 10+ sibling EvenHub apps (rdt, weather, tetris, snake, etc.).
  </action>
  <verify>
    <automated>node -e "const a = require('./app.json'); const ok = a.package_id === 'lv.kingdom.bibele.openclawchat' && a.entrypoint === 'index.html' && a.permissions.network[0] === '*' && a.edition === '202601'; console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1)" && node -e "const p = require('./package.json'); const ok = p.devDependencies['@evenrealities/evenhub-cli'] && p.scripts.pack && p.scripts.pack.includes('evenhub pack'); console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <done>app.json exists with valid metadata that passes evenhub pack schema validation. package.json has @evenrealities/evenhub-cli in devDependencies and a "pack" script that chains build + evenhub pack.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite for production build and verify full pack pipeline</name>
  <files>vite.config.ts</files>
  <action>
1. Modify `vite.config.ts` to use the functional form of `defineConfig` with the `command` parameter:
   - Change `defineConfig({` to `defineConfig(({ command }) => {`
   - Add `base: './'` to produce relative asset paths (EvenHub WebView requires this)
   - Make the simulator rollup input conditional: only include `preview-glasses.html` when `command === 'serve'` (dev server), exclude it when `command === 'build'` (production)
   - Preserve all existing config: root, resolve alias, outDir, server port/open, test config
   - Return the config object from the function

The resulting vite.config.ts should match the pattern from 08-RESEARCH.md "Modified vite.config.ts" code example.

2. Run `npm run build` and verify:
   - `dist/index.html` exists
   - `dist/preview-glasses.html` does NOT exist
   - Asset paths in `dist/index.html` use `./assets/` (relative) not `/assets/` (absolute)

3. Run `npm run pack` and verify:
   - Command completes successfully
   - `openclaw-chat.ehpk` file is produced
   - The .ehpk file has non-zero size

4. Run existing tests to confirm nothing is broken:
   - `npm test` passes (all 247 tests)
  </action>
  <verify>
    <automated>npm run build && test -f dist/index.html && test ! -f dist/preview-glasses.html && grep -q '\./assets/' dist/index.html && npm run pack && test -f openclaw-chat.ehpk && npm test</automated>
  </verify>
  <done>Production build excludes simulator and uses relative paths. npm run pack produces openclaw-chat.ehpk. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `app.json` passes evenhub pack validation (validated by successful pack)
2. `dist/` contains only `index.html` and `assets/` (no `preview-glasses.html`)
3. `dist/index.html` references `./assets/` not `/assets/`
4. `openclaw-chat.ehpk` exists with non-zero size
5. All 247 existing tests pass
6. Dev server (npm run dev) still serves preview-glasses.html
</verification>

<success_criteria>
- app.json defines complete EvenHub package metadata with valid package_id, edition, permissions
- Production Vite build is self-contained with relative paths and no dev-only files
- `npm run pack` produces a valid .ehpk submission artifact
- Simulator remains accessible during development via dev server
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-evenhub-submission/08-01-SUMMARY.md`
</output>
