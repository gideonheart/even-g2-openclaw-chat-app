---
phase: 01-v1-4-voice-loop-polish-decoupled-input-voice-queue
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/gestures/gesture-handler.ts
  - src/__tests__/gesture-handler.test.ts
  - src/gestures/gesture-fsm.ts
  - src/voice-loop-controller.ts
autonomous: false
requirements: [VLQ-05, VLQ-07, VLQ-08]

must_haves:
  truths:
    - "Hint text for sent state says 'Processing... | Tap to record next' (reflects tappable state)"
    - "npm run typecheck passes with zero errors after all changes"
    - "npm test passes with zero failures (full suite, no regressions)"
    - "No dead code or obsolete comments remain in gesture-fsm.ts, gesture-handler.ts, or voice-loop-controller.ts"
    - "Transcript-first rendering on glasses is not regressed (display-controller test still passes)"
  artifacts:
    - path: "src/gestures/gesture-handler.ts"
      provides: "Updated hint text for sent state"
      contains: "Tap to record next"
    - path: "src/gestures/gesture-fsm.ts"
      provides: "Clean transition table with accurate comments"
      contains: "Tap starts a new recording"
  key_links:
    - from: "src/gestures/gesture-handler.ts"
      to: "src/gestures/gesture-fsm.ts"
      via: "getHintText returns text matching FSM state semantics"
      pattern: "Processing.*Tap to record"
---

<objective>
Polish the codebase after the state-aware reset and voice turn queue changes: update hint text, remove dead code and obsolete comments, verify type safety, and run full regression suite.

Purpose: Plans 01 and 02 made behavioral changes. This plan ensures the codebase is clean, hints reflect the new tappable-in-sent-state behavior, obsolete comments are removed, and the full suite confirms zero regressions including transcript rendering.

Output: Clean, polished codebase with updated hints, no dead code, passing typecheck and full test suite.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v1-4-voice-loop-polish-decoupled-input-voice-queue/01-RESEARCH.md
@.planning/phases/01-v1-4-voice-loop-polish-decoupled-input-voice-queue/01-01-SUMMARY.md
@.planning/phases/01-v1-4-voice-loop-polish-decoupled-input-voice-queue/01-02-SUMMARY.md
@src/gestures/gesture-handler.ts
@src/gestures/gesture-fsm.ts
@src/voice-loop-controller.ts
@src/__tests__/gesture-handler.test.ts
@src/__tests__/display-controller.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hint text, remove dead code, audit comments across all modified files</name>
  <files>src/gestures/gesture-handler.ts, src/__tests__/gesture-handler.test.ts, src/gestures/gesture-fsm.ts, src/voice-loop-controller.ts</files>
  <action>
1. **Update hint text for `sent` state** in `src/gestures/gesture-handler.ts`:
   - In the `getHintText` function, change the `case 'sent'` return value from `'Processing...'` to `'Processing... | Tap to record next'`
   - This reflects that tapping is now allowed in sent state (Quick-19/20 FSM change)

2. **Update hint text test** in `src/__tests__/gesture-handler.test.ts`:
   - In the `getHintText` describe block, update the test `returns correct text for sent` to expect `'Processing... | Tap to record next'` instead of `'Processing...'`

3. **Audit gesture-fsm.ts comments** in `src/gestures/gesture-fsm.ts`:
   - Verify the `sent` block comment (around line 44-45) says something like "Tap starts a new recording while previous turn is processing" (already correct after Quick-20, but verify no stale "all inputs ignored" comment remains)
   - Verify the `thinking` block has an accurate comment about tap being allowed
   - Remove any comments that say inputs are "ignored" or "blocked" in sent/thinking states (those are no longer true)

3.5. **Document unreachable `thinking` state** in `src/gestures/gesture-fsm.ts`:
   - The `thinking` state has transitions defined (lines 47-52) but no transition in the table produces `nextState: 'thinking'`
   - The comment on line 45 says "auto-transitions to 'thinking' externally via event bus" but no such code exists
   - Add a clarifying comment above the `thinking` block: `// NOTE: No FSM transition currently produces nextState 'thinking'. This state exists for forward compatibility. Display-controller uses a separate IconState type for the thinking icon.`
   - Do NOT remove the `thinking` state -- it would cascade to the GestureState type used by the icon animator and other modules. Removal is out of scope for this phase.

4. **Audit gesture-handler.ts comments**:
   - The file header comment (lines 1-6) mentions "Phase 2 scope: produces hint text strings only." -- this is now outdated since Phase 3 has shipped. Update to remove the "Phase 2 scope" reference, or replace with a generic accurate description.
   - The `getHintText` JSDoc (line 24-26) says "Phase 3 will call this" -- Phase 3 is done. Update to accurate present tense.
   - Verify the gateway:chunk listener comment accurately describes the state-aware behavior after Plan 01's changes.

5. **Audit voice-loop-controller.ts comments**:
   - The file header comment should mention the voice turn queue (update the module doc comment to mention sequential queuing)
   - Verify no stale "When recording stops, send voice turn to gateway" comments remain (should now say "enqueue")

5.5. **Document queue independence from FSM state** in `src/voice-loop-controller.ts`:
   - Add a comment near the queue drain logic: `// Queue state (busy flag, pendingTurns) is independent of the gesture FSM state. If a watchdog reset fires in gesture-handler (resetting FSM to idle), the queue continues to drain correctly when response_end/error arrives.`

6. **Run typecheck**: `npx tsc --noEmit` must pass with zero errors.

7. **Run display-controller tests** to verify transcript-first rendering is not regressed:
   - `npx vitest run src/__tests__/display-controller.test.ts` -- all tests must pass
   - This verifies VLQ-05 (transcript rendering preserved)
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
    <manual>Verify hint text for sent state says "Processing... | Tap to record next" and no stale phase references remain in comments</manual>
  </verify>
  <done>Hint text updated for sent state. All stale comments removed/updated. Typecheck passes. Full test suite passes with zero regressions. Display-controller tests confirm transcript rendering preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete voice loop behavior</name>
  <files>src/gestures/gesture-handler.ts, src/voice-loop-controller.ts, src/gestures/gesture-fsm.ts</files>
  <action>
This is a human verification checkpoint. All implementation is complete from Plans 01, 02, and Task 1 above. Present the following verification steps to the user.

Complete voice loop decoupling and queue implementation:
1. State-aware reset guard: response_end/error only reset FSM from sent/thinking states (recording is immune)
2. Voice turn queue: multiple recordings execute sequentially without aborting in-flight requests
3. Updated hint text: sent state shows "Processing... | Tap to record next"
4. Clean codebase: no dead code, stale comments removed, typecheck clean
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx vitest run 2>&1 | tail -5</automated>
    <manual>
1. Run the full test suite: `npx vitest run` -- Expected: all tests pass (~590+ tests)
2. Run typecheck: `npx tsc --noEmit` -- Expected: no errors
3. Review gesture-handler.ts: gateway:chunk listener has state guard (`state === 'sent' || state === 'thinking'`)
4. Review voice-loop-controller.ts: has pendingTurns, busy flag, processQueue, queue drain on response_end/error
5. Optional real hardware test: record during Processing state, verify second turn queues and sends after first completes
    </manual>
  </verify>
  <done>User has verified the complete voice loop implementation: state-aware reset, voice turn queue, updated hints, clean codebase. All tests pass, typecheck clean.</done>
</task>

</tasks>

<verification>
Final verification after all three plans complete:

```bash
cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app
npx tsc --noEmit
npx vitest run
```

Verify:
- typecheck: 0 errors
- tests: 0 failures, test count increased from 582 baseline
- gesture-handler: state-aware reset guard, updated hint text
- voice-loop-controller: FIFO queue, sequential drain, destroy cleanup
- display-controller: transcript rendering preserved (no regression)
</verification>

<success_criteria>
- Hint text for sent state is "Processing... | Tap to record next"
- No stale "Phase 2 scope" or "Phase 3 will" comments in gesture-handler.ts
- No stale "all inputs ignored" comments in gesture-fsm.ts
- voice-loop-controller.ts header comment mentions sequential queue
- `npx tsc --noEmit` passes with 0 errors
- `npx vitest run` passes with 0 failures
- display-controller tests pass (VLQ-05 transcript preservation)
- User approves the complete implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-v1-4-voice-loop-polish-decoupled-input-voice-queue/01-03-SUMMARY.md`
</output>
