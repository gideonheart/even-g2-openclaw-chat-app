---
phase: 02-even-bridge-input-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/gestures/gesture-fsm.ts
  - src/__tests__/gesture-fsm.test.ts
autonomous: true
requirements:
  - GEST-01
  - GEST-02
  - GEST-03
  - GEST-04
  - GEST-05
  - VOICE-01
  - VOICE-02

must_haves:
  truths:
    - "gestureTransition('idle', 'tap') returns nextState:'recording' with START_RECORDING action"
    - "gestureTransition('recording', 'tap') returns nextState:'sent' with STOP_RECORDING action"
    - "gestureTransition('idle', 'double-tap') returns nextState:'menu' with TOGGLE_MENU action"
    - "gestureTransition('menu', 'double-tap') returns nextState:'idle' with TOGGLE_MENU action"
    - "gestureTransition('idle', 'scroll-up') returns SCROLL_UP action staying in idle"
    - "gestureTransition('idle', 'scroll-down') returns SCROLL_DOWN action staying in idle"
    - "gestureTransition('recording', 'double-tap') returns null action, stays recording"
    - "gestureTransition('sent', any input) returns null action, stays sent"
  artifacts:
    - path: "src/gestures/gesture-fsm.ts"
      provides: "Pure gesture state machine with GestureState, GestureInput, GestureAction types and gestureTransition function"
      exports: ["GestureState", "GestureInput", "GestureAction", "GestureTransition", "gestureTransition"]
    - path: "src/__tests__/gesture-fsm.test.ts"
      provides: "Full test coverage for all FSM state transitions"
      min_lines: 80
  key_links:
    - from: "src/gestures/gesture-fsm.ts"
      to: "src/types.ts"
      via: "exports gesture types consumed by gesture-handler in Plan 03"
      pattern: "export type Gesture(State|Input|Action)"
---

<objective>
Implement the pure gesture finite state machine as a TDD plan — RED tests first, then GREEN implementation.

Purpose: The gesture FSM is the core decision logic for mapping all 4 Even G2 gestures (tap, double-tap, scroll-up, scroll-down) to application actions (START_RECORDING, STOP_RECORDING, TOGGLE_MENU, SCROLL_UP, SCROLL_DOWN). It must be a pure function with zero side effects so it remains fully testable without the SDK or hardware. This is the foundation that the gesture handler (Plan 03) wires to the event bus.

Output: `src/gestures/gesture-fsm.ts` with exported types and `gestureTransition()` pure function, fully tested.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-even-bridge-input-pipeline/02-RESEARCH.md
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing gesture FSM tests</name>
  <files>src/__tests__/gesture-fsm.test.ts</files>
  <action>
Create `src/__tests__/gesture-fsm.test.ts` that imports `gestureTransition`, `GestureState`, `GestureInput` from `../gestures/gesture-fsm` (file does not exist yet — tests must fail).

Write tests covering all state transitions from the RESEARCH.md Pattern 2 transition table:

**idle state:**
- tap → { nextState: 'recording', action: { type: 'START_RECORDING' } }
- double-tap → { nextState: 'menu', action: { type: 'TOGGLE_MENU' } }
- scroll-up → { nextState: 'idle', action: { type: 'SCROLL_UP' } }
- scroll-down → { nextState: 'idle', action: { type: 'SCROLL_DOWN' } }

**recording state:**
- tap → { nextState: 'sent', action: { type: 'STOP_RECORDING' } }
- double-tap → { nextState: 'recording', action: null } (ignored during recording)
- scroll-up → { nextState: 'recording', action: null } (ignored during recording)
- scroll-down → { nextState: 'recording', action: null } (ignored during recording)

**sent state:** (all inputs ignored, auto-transitions to thinking externally)
- tap → { nextState: 'sent', action: null }
- double-tap → { nextState: 'sent', action: null }

**thinking state:**
- double-tap → { nextState: 'menu', action: { type: 'TOGGLE_MENU' } }
- tap → { nextState: 'thinking', action: null } (ignored)
- scroll-up → { nextState: 'thinking', action: null } (ignored)

**menu state:**
- double-tap → { nextState: 'idle', action: { type: 'TOGGLE_MENU' } }
- tap → { nextState: 'idle', action: null } (dismiss menu)
- scroll-up → { nextState: 'menu', action: { type: 'SCROLL_UP' } }
- scroll-down → { nextState: 'menu', action: { type: 'SCROLL_DOWN' } }

Also test unknown state/input fallback: returns { nextState: currentState, action: null }.

Create a minimal stub file `src/gestures/gesture-fsm.ts` that exports the types but throws `'not implemented'` from `gestureTransition` so the test file compiles but tests fail (RED phase).

Run tests: `npx vitest run src/__tests__/gesture-fsm.test.ts` — expect ALL to fail.
Commit: `test(02-01): add failing gesture FSM tests (RED)`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gesture-fsm.test.ts 2>&1 | tail -5</automated>
    <manual>All tests should FAIL (red phase). The test file should compile without TypeScript errors.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Test file exists with 15+ test cases covering all 5 states. All tests fail because gestureTransition is not implemented.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Implement gestureTransition pure function</name>
  <files>src/gestures/gesture-fsm.ts</files>
  <action>
Replace the stub in `src/gestures/gesture-fsm.ts` with the full implementation:

1. Export types: `GestureState` (union: 'idle' | 'recording' | 'sent' | 'thinking' | 'menu'), `GestureInput` (union: 'tap' | 'double-tap' | 'scroll-up' | 'scroll-down'), `GestureAction` (discriminated union of { type: 'START_RECORDING' } | { type: 'STOP_RECORDING' } | { type: 'TOGGLE_MENU' } | { type: 'SCROLL_UP' } | { type: 'SCROLL_DOWN' } | null), `GestureTransition` ({ nextState: GestureState; action: GestureAction }).

2. Define `TRANSITIONS` as a `Record<GestureState, Partial<Record<GestureInput, GestureTransition>>>` constant following the exact transition table from RESEARCH.md Pattern 2.

3. Export `gestureTransition(state: GestureState, input: GestureInput): GestureTransition` — looks up in TRANSITIONS table, returns fallback `{ nextState: state, action: null }` if no entry found.

This is a pure function with zero imports and zero side effects.

Run tests: `npx vitest run src/__tests__/gesture-fsm.test.ts` — expect ALL to pass.
Commit: `feat(02-01): implement gesture FSM pure function (GREEN)`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gesture-fsm.test.ts</automated>
    <manual>All 15+ tests should PASS. No TypeScript errors.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All gesture FSM tests pass. gestureTransition correctly maps all state/input combinations. Types exported for downstream consumers.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/gesture-fsm.test.ts` — all tests pass
- `npx tsc --noEmit` — no TypeScript errors
- `src/gestures/gesture-fsm.ts` exports GestureState, GestureInput, GestureAction, GestureTransition, gestureTransition
- gestureTransition is a pure function (no imports, no side effects, no async)
</verification>

<success_criteria>
- Pure gesture FSM with all 5 states and 4 inputs fully tested
- All transitions match the RESEARCH.md specification
- Types exported for Plan 03 gesture handler consumption
- Full test suite (`npx vitest run`) passes including all existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-even-bridge-input-pipeline/02-01-SUMMARY.md`
</output>
