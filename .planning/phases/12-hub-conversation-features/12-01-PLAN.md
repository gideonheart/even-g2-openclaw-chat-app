---
phase: 12-hub-conversation-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sync/sync-types.ts
  - src/persistence/auto-save.ts
  - src/hub-main.ts
  - index.html
  - src/__tests__/auto-save.test.ts
autonomous: true
requirements:
  - HUB-01
  - HUB-02

must_haves:
  truths:
    - "User opens hub and sees the active conversation's messages rendered in a live view panel on the home page"
    - "When glasses receives a new message (user or assistant), it appears in the hub live view within one second"
    - "While the assistant is responding, the hub shows a streaming indicator (typing dots or similar)"
    - "Hub loads existing conversation messages from IndexedDB on boot (not just real-time messages)"
  artifacts:
    - path: "src/sync/sync-types.ts"
      provides: "streaming:start and streaming:end SyncMessage types"
      contains: "streaming:start"
    - path: "src/hub-main.ts"
      provides: "Live conversation rendering and streaming indicator logic"
      contains: "appendLiveMessage"
    - path: "index.html"
      provides: "Live conversation panel DOM with streaming indicator element"
      contains: "liveConversation"
  key_links:
    - from: "src/persistence/auto-save.ts"
      to: "src/sync/sync-types.ts"
      via: "posts streaming:start and streaming:end sync messages"
      pattern: "streaming:start"
    - from: "src/hub-main.ts"
      to: "src/persistence/conversation-store.ts"
      via: "loads existing messages on boot via getMessages()"
      pattern: "getMessages"
    - from: "src/hub-main.ts"
      to: "src/sync/sync-types.ts"
      via: "handles message:added and streaming sync messages from glasses"
      pattern: "message:added"
---

<objective>
Add a live conversation view to the hub home page that displays the active glasses conversation in real time, with a streaming indicator while the assistant is responding.

Purpose: Users need to see what's happening on the glasses from the hub -- both the existing conversation history and new messages as they arrive in real time.
Output: Live conversation panel on hub home page with real-time sync and streaming indicator.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-hub-conversation-features/12-RESEARCH.md
@.planning/phases/10-dynamic-sessions-cross-context-sync/10-03-SUMMARY.md
@.planning/phases/13-phase9-verification-sync-wiring/13-01-SUMMARY.md
@src/sync/sync-types.ts
@src/persistence/auto-save.ts
@src/hub-main.ts
@index.html
@src/persistence/types.ts
@src/persistence/conversation-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add streaming sync messages and wire into auto-save</name>
  <files>src/sync/sync-types.ts, src/persistence/auto-save.ts, src/__tests__/auto-save.test.ts</files>
  <action>
1. In `src/sync/sync-types.ts`, add two new SyncMessage union members:
   - `{ type: 'streaming:start'; origin: SyncOrigin; conversationId: string }` -- posted when assistant starts responding
   - `{ type: 'streaming:end'; origin: SyncOrigin; conversationId: string }` -- posted when assistant response completes

2. In `src/persistence/auto-save.ts`, wire the new sync messages into the existing `gateway:chunk` handler:
   - On `response_start` chunk: post `streaming:start` sync message via syncBridge (if available), using `getConversationId()` for the conversationId
   - On `response_end` chunk (where assistant text is already saved): post `streaming:end` sync message AFTER the successful save (inside the existing `.then(ok => ...)` block, alongside the existing `message:added` post)
   - On `error` chunk: also post `streaming:end` sync message (if syncBridge available) to clear any active streaming indicator

3. In `src/__tests__/auto-save.test.ts`, add tests:
   - Test that `streaming:start` sync message is posted on `response_start` chunk when syncBridge is provided
   - Test that `streaming:end` sync message is posted on `response_end` chunk after successful save
   - Test that `streaming:end` sync message is posted on `error` chunk (cleanup)
   - Test that no streaming sync messages are posted when syncBridge is not provided (backward compat)
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx vitest run src/__tests__/auto-save.test.ts --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Verify new streaming sync types compile clean and tests cover all three chunk events</manual>
  </verify>
  <done>streaming:start and streaming:end SyncMessage types exist. Auto-save posts them on response_start, response_end, and error. All auto-save tests pass including new streaming tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add live conversation panel to hub with real-time sync and streaming indicator</name>
  <files>src/hub-main.ts, index.html</files>
  <action>
1. In `index.html`, add a live conversation panel to the home page, AFTER the "Glasses" connection card and BEFORE "Quick actions":
   ```html
   <div class="page-section" id="liveConversationSection">
     <div class="section-title">Live conversation</div>
     <div class="card" style="padding: 0; overflow: hidden;">
       <div id="liveConversation" style="max-height: 300px; overflow-y: auto; padding: 12px;">
         <div class="u-type-body-base u-tc-2nd" style="text-align: center;" id="liveEmpty">No active conversation</div>
       </div>
       <div id="streamingIndicator" class="hidden" style="padding: 8px 12px; border-top: 1px solid var(--sc-2nd); color: var(--tc-2nd); font-size: var(--text-title-xs-size);">
         Assistant is typing...
       </div>
     </div>
   </div>
   ```

2. Add CSS for chat messages in `index.html` `<style>` section:
   ```css
   .chat-msg { padding: 6px 10px; margin-bottom: 4px; border-radius: var(--radius-base); font-size: var(--text-body-base-size); line-height: 1.4; word-break: break-word; }
   .chat-msg--user { background: var(--bc-highlight); color: var(--tc-highlight); margin-left: 40px; text-align: right; }
   .chat-msg--assistant { background: var(--bc-2nd); color: var(--tc-1st); margin-right: 40px; }
   ```

3. In `src/hub-main.ts`, add the following functions:

   a. `appendLiveMessage(role: string, text: string)` -- creates a div with class `chat-msg chat-msg--{role}`, sets `textContent` to `text` (XSS-safe), appends to `$('liveConversation')`, removes the `liveEmpty` placeholder if visible, auto-scrolls to bottom via `container.scrollTop = container.scrollHeight`.

   b. `clearLiveView()` -- clears `$('liveConversation').innerHTML` and restores the `liveEmpty` placeholder.

   c. `showStreamingIndicator()` -- removes `hidden` class from `$('streamingIndicator')`.

   d. `hideStreamingIndicator()` -- adds `hidden` class to `$('streamingIndicator')`.

   e. `loadLiveConversation()` -- async function that:
      - Gets active session ID from `sessionManager.getActiveSessionId()`
      - If no session manager or no active session, returns early
      - Needs access to `conversationStore` -- add a module-level `hubConversationStore` variable (set from `initPersistence`)
      - Calls `hubConversationStore.getMessages(activeId)` to load all messages
      - Calls `clearLiveView()` then loops over messages calling `appendLiveMessage(msg.role, msg.text)` for each
      - Hides streaming indicator

4. In `src/hub-main.ts` `initPersistence()`:
   - Store the `conversationStore` reference to the module-level `hubConversationStore` variable so `loadLiveConversation()` can access it
   - Fill in the `message:added` stub case to call `appendLiveMessage(msg.role, msg.text)` and if `msg.role === 'assistant'` also call `hideStreamingIndicator()`
   - Add `streaming:start` case: call `showStreamingIndicator()`
   - Add `streaming:end` case: call `hideStreamingIndicator()`
   - Add `session:switched` case to the sync handler: call `loadLiveConversation()` (so switching sessions reloads the live view)

5. In `src/hub-main.ts` `initHub()`:
   - After persistence is initialized and active session set, call `await loadLiveConversation()` to load existing messages

Note: Use `textContent` (not `innerHTML`) for message text to prevent XSS. The `escHtml` utility is not needed when using `textContent`.
  </action>
  <verify>
    <automated>cd /home/forge/bibele.kingdom.lv/samples/even-g2-openclaw-chat-app && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
    <manual>Open hub in browser, verify live conversation panel appears on home page. If a conversation exists in IndexedDB, messages should display. Check that streaming indicator element exists but is hidden.</manual>
  </verify>
  <done>Hub home page shows live conversation panel. Existing messages load from IndexedDB on boot. New messages from glasses appear in real time via sync. Streaming indicator shows/hides based on streaming:start/streaming:end sync messages. All tests pass, TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npx vitest run` -- all tests pass (existing + new auto-save streaming tests)
3. Hub home page shows "Live conversation" card with messages from active session
4. When glasses sends a message (via sync bridge), it appears in the hub live view
5. Streaming indicator appears between user message and assistant message completion
</verification>

<success_criteria>
- HUB-01: Hub displays live glasses conversation -- messages load from IDB on boot, new messages arrive via SyncBridge message:added
- HUB-02: Streaming indicator visible while assistant responds -- driven by streaming:start/streaming:end sync messages
- All existing 352+ tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-hub-conversation-features/12-01-SUMMARY.md`
</output>
