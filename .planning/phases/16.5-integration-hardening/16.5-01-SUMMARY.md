---
phase: 16.5-integration-hardening
plan: 01
subsystem: persistence
tags: [indexeddb, reopendb, eviction, cleanup, teardown, glasses]

# Dependency graph
requires:
  - phase: 14-data-integrity-foundation
    provides: reopenDB, setOnUnexpectedClose, integrity-checker, storage-health, eviction sentinel
  - phase: 16-sync-hardening
    provides: driftReconciler, syncMonitor, heartbeat infrastructure
provides:
  - "reopenDB success path propagates new IDBDatabase to all 5 IDB-dependent modules (store, sessionStore, autoSave, driftReconciler, syncMonitor)"
  - "storage:evicted event produces user-visible renderer.showError after display init via flag pattern"
  - "cleanup teardown calls driftReconciler.destroy() before syncMonitor.destroy()"
affects: [16.5-02, 18-error-ux]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Store refresh after reopenDB: destroy and recreate all IDB-dependent modules from fresh handle"
    - "Eviction flag pattern: set boolean during boot, check after renderer init (avoids missed synchronous events)"

key-files:
  created: []
  modified:
    - src/glasses-main.ts

key-decisions:
  - "Recreate all 5 IDB-dependent modules after reopenDB instead of partial store-only refresh (correctness over simplicity)"
  - "Use flag pattern for eviction notification (evictionDetected boolean) instead of bus subscriber (event fires before renderer exists)"
  - "driftReconciler.destroy() placed before syncMonitor.destroy() to clear mismatch counter before stopping heartbeat"

patterns-established:
  - "Store refresh after reopenDB: full destroy+recreate cycle for autoSave, driftReconciler, syncMonitor"
  - "Eviction flag pattern: set flag during synchronous boot event, render notification after async display init"

requirements-completed: [RES-15, RES-04, RES-11]

# Metrics
duration: 4min
completed: 2026-02-28
---

# Phase 16.5 Plan 01: Glasses Integration Hardening Summary

**Three glasses-main.ts integration fixes: reopenDB handle propagation to all 5 IDB-dependent modules, storage eviction flag-based notification via renderer.showError, and driftReconciler cleanup ordering in teardown**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-28T23:16:33Z
- **Completed:** 2026-02-28T23:20:57Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- reopenDB success handler now accepts newDb and recreates store, sessionStore, autoSave, driftReconciler, and syncMonitor from the fresh IDBDatabase handle
- evictionDetected flag is set during sentinel check (boot) and renderer.showError('Data was cleared by system') is called after display init
- cleanup() now calls driftReconciler?.destroy() before syncMonitor?.destroy() in correct teardown order
- All 436 existing tests pass with zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix reopenDB handle propagation and eviction subscriber** - `e711eb8` (feat)

## Files Created/Modified
- `src/glasses-main.ts` - Three integration fixes: reopenDB handle propagation, eviction subscriber, cleanup teardown ordering

## Decisions Made
- Recreate all 5 IDB-dependent modules (store, sessionStore, autoSave, driftReconciler, syncMonitor) after reopenDB instead of only refreshing stores -- autoSave/driftReconciler/syncMonitor capture store in closure at creation time, so they must be destroyed and recreated
- Used flag pattern (evictionDetected boolean) for eviction notification because storage:evicted event fires synchronously during boot before the renderer exists
- Changed const to let for autoSave, driftReconciler, and syncMonitor declarations to allow reassignment in reopenDB handler

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Glasses-side integration fixes complete, ready for Plan 02 (hub-side fixes)
- Hub-main.ts already has uncommitted changes staged from research that align with Plan 02 scope
- Phase 18 Error UX can now consume reliable persistence:error and storage:evicted signals from the glasses context

## Self-Check: PASSED

- [x] src/glasses-main.ts exists
- [x] 16.5-01-SUMMARY.md exists
- [x] Commit e711eb8 exists

---
*Phase: 16.5-integration-hardening*
*Completed: 2026-02-28*
